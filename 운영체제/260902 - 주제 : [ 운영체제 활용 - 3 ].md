260902 - 주제 : \[ 운영체제 활용 - 3 ]



==================================================

\[김정인 강사님] 코딩 \& 개념 노트

==================================================

\--------------------------------------------------

■ 1. 오늘 배운 주요 단어 / 문법

\--------------------------------------------------

(형식: \[단어/명령어] : 의미 - 부연설명)



\* \[ execve의 3단계 ] : **inode의 x 비트 검사**(**rw-r--r--와 같은 권한**만 확인) >

&#x20;**첫 2바이트** 읽기(**#!**' 이면 **스크립트**. **뒤의 경로**가 **인터프리터**) >

인터프리터로드 (/bin/bash 를 대신 exec, argv = \[/bin/bash,./run.sh])



\* \[ **bash run.sh** ] : 실행 대상이 /bin/bash → **커널은 /bin/bash의 x만 검사**.

&#x09;		**run.sh는 r만 있으면 됨**.



\* \[ **bash의 루프** ] : **fork** (자식= bash 복사본) > **execve** (자식의몸이ls로) > **wait** ( 부모 멈춤 ) >

**exit** (종료코드→ 커널) > **$?** (부모가 받았는지 작동 확인 및 다음 줄)



$? — 0이 성공

ls -l → 0  		- 성공

ls /nonexist → 2	- 실패

**0이 아니면 실패**



**cd 명령을 별도 프로세스(fork)로 실행**하면 **자식의 위치만 변경**될 뿐

따라서 **셸 자신의 위치를 바꾸려면** **bash가 직접 내장 명령(chdir)을 실행**해야 함.



|**./collect.sh**|fork+execve → <br />자식 bash|자식의상자에LOG\_DIR 생김|자식종료 → 상자소멸|터미널: echo $LOG\_DIR → 빈 값|
|-|-|-|-|-|
|**source collect.sh**|fork 없음|터미널 bash가 직접 읽어 실행|터미널 상자에LOG\_DIR 남음|(.bashrc 를 source로 읽는 이유)|
|**export LOG\_DIR=...**|execve 때 envp로 자식에 복사|자식이LOG\_DIR=/tmp 로 바꿔도|**부모는 그대로**|**흐름은 부모→ 자식한 방향**|



\* \[ **쉘 변수** ] : 자식에게 변수를 전달하지 않음

name = ??로 변수 저장 이후 echo $name과 같이 $를 붙여 쉘 변수를 빼낼 수 있음

하지만 bash로 자식 터미널로 접속할 경우 $를 입력해도 name을 불러오지 못함



\* \[ **환경변수(export변수)** ] : 자식이 부모에게 전달받을 수 있는 변수 (**부모에서 자식**으로 **단방향으로** 향하는 변수)



\* \[ **파이프** ] : **커널** 버퍼 하나에 두 프로세스가 꽂힌다

&#x20; > 활용 예시 :

sensor (601)  fd 1 > 파이프(쓰기) 		- 정상일 경우 **둘이 동시에 실행**.

&#x09;						  sensor가 write > 버퍼 > grep이 read > 필터 > 파일

&#x09;	    v



파이프 버퍼 (커널,**64KB**)  채움 3KB/64KB	- **버퍼가 가득 찬 경우 write가** **블록,** **커널**이 sensor를 재움.

grep이 읽으면 꺠어남 = 흐름 제어		  							데이터는 안 버려짐.

&#x09;	    v

grep WARN(602)						   -오른쪽 먼저 종료인 경우 **head -1** 이 먼저 끝나면

fd > 0 파이프 읽기 fd 1 > log.		 읽기 끝 닫힘 > sensor의 다음 write > **SIGPIPE** > 조용히 종료



**파이프 양쪽 프로세스**가 각자 **stdout/stdin을 버퍼에 꽂을 때** 쓰는 시스템 콜 : **dup2**



\* \[ **SSH 끊는 3가지?** ] : **SIGHUP · nohup · setsid**

|**./collect.sh \&**|같은세션·같은제어터미널|**SSH 끊김** → **커널**이 **세션에 SIGHUP**|기본처리기 = 종료|**재접속 후 ps → <br />없음**|
|-|-|-|-|-|
|**nohup ./collect.sh \&**|SIGHUP 처리기를 '무시'로 바꾼 뒤 exec|출력은 nohup.out|bash는 죽고 스크립트는 생존|**재접속 후 ps → <br />있음 ( 터미널 꺼도 돌아간다 )**|
|**setsid ./collect.sh \&**|새 세션(SID)의 리더, 제어 터미널 없음|커널은 옛 세션에만 SIGHUP|신호 자체를 받지 않음|**= systemd 서비스가 사는 방식**|

**SID는 출력자체가 안되기 때문에 log로 남기는 편임.**



\* \[ **크론(cron)** ] : 매 분 깨어나 다섯 칸을 대조하는 데몬

|분|시|일|월|요일|
|-|-|-|-|-|
|0-59|0-23|1-31|1-12|0-7|



\* \* \* \* \*  /home/user/collect.sh     ← 매 분 실행

0 9 \* \* 1  /home/user/backup.sh   ← 월요일 09:00 실행



**터미널에선 되는데 cron에선 안 됨' 1순위**

python3 가 /usr/local/bin 에 있으면 **cron의 PATH로는 못 찾음** → exit

127, 오류는 메일함/syslog로.

**해결법**

**절대경로**(/usr/local/bin/python3) 또는 crontab 맨 위에 **PATH=… 한 줄**



\* \[ **systemd** ] : 부팅 시 커널이 띄우는 **첫 프로세스(PID 1)**



\* \[ **After=network.target** ] : 네트워트가 먼저



\* \[ **restart** ] : **서비스가 죽었을 때 systemd가 다시 띄우게 하는 unit 옵션**



\* \[ **enable** ] : 부팅 때 **자동으로 시작**되도록 등록하는 **systemctl 하위 명령**







\* \[ **collect.sh 과정** ] :

**1.systemd (PID 1)**

collect\_loop.sh를 fork/exec — 부모= PID 1

&#x09;			**v**

**2.execve + #!**

x 비트 검사→ /bin/bash 로드

&#x09;			**v**

**3.한줄마다fork·exec·wait**

$? 로 다음 줄 결정

&#x09;			**v**

**4.변수상자**

자식bash 안에서만존재

&#x09;			**v**

**5.| 와 >>**

dup2 로 fd 0·1 화살표 교체

&#x09;			**v**

**6.open(sensor.log)**

dentry → inode → struct file → fd3

&#x09;			v

**7.write → 페이지 캐시**

dirty → writeback → 디스크

&#x09;			v

**8.세션없음**

SIGHUP 무관, 죽으면 Restart

\--------------------------------------------------

■ 2. 핵심 코드 \& 로직 흐름

\--------------------------------------------------

\[구현 목표 1 : 리눅스 명령어에 대하여]

↓↓↓ Code ↓↓↓


```
\[ -d \~/logs ] - **홈 디렉토리에 logs가 있는가?**



\[ -d \~/logs ] **||** mkdir -p \~/logs	-  **디렉토리 logs가 있으면 mkdir 실행 없으면 실행 X  ||가 판가름**



cp a.txt b.txt \&\& echo 복사됨 		- **a.txt와 b.txt가 있다면 echo 복사됨 실행**



touch ??  							**- ??  빈 파일을 생성 aa.txt와 같은거**



if cmd; then … fi					**- cmd의 $?가 0이면 then 실행**





\#!/bin/bash							- **bash 자식에서 실행**





LOG\_DIR=\~/logs                            		- **변수 LOG\_DIR의 폴더 선언**

echo "$LOG\_DIR"						- **변수 LOG\_DIR 내부 값 출력**

\[ -d "$LOG\_DIR" ] || mkdir -p "$LOG\_DIR"  	- **LOG\_DIR이 참이 아닐 때만 LOG\_DIR 값과 같은 파일 생성**

for i in 1 2 3; do						- **이하 종속값 3회 실행**

date >> "$LOG\_DIR/test.log"			- **곧바로 현재 날짜 및 시간 입력**

done



{

cat /sys/class/thermal/thermal\_zone0/temp \\ - **시스템 CPU/온도 센서 파일 읽고 \\는 줄 바꾸고 계속 이어짐을 의미**

| awk -v t="$(date '+%F %T')" '{printf "%s temp=%.1fC\\n", t, $1/1000}'

\*\*|\*\*는 **앞 명령어의 출력값**을 뒤 **명령어(awk)로 전송**을 의미, 변수 **t**에 **현재 날짜 및 시간 입력**,

**읽어온 온도를 섭씨(℃)로 계산하여 시간과 함께 정형화 출력**

} >> "$LOG\_DIR/sensor.log" 2>\&1 - **출력 결과**를 **sensor.log 파일에 추가 저장**

2>\&1							-**표준 에러(2)를 표준 출력(1)이 향하는 곳으로 보내라**



p | awk '{print $2} 								- **2번 컬럼만 추출하겠다. awk**



stty -a							- **단축기 확인**

:!명령어										**- 잠시 터미널로 이동 후 명령어 확인**

ctrl+z , fg										**- vi를 멈추고 터미널로 이동, fg로 다시 vi로 복귀**



ps -ef | grep





**PATH=/user/local/bin:/usr/bin:/bin				- 경로 고정**

**LOG\_DIR=/home/ubuntu/logs					-log\_dir 위치 filezila 참고해서 설정**

**\[ -d "$LOG\_DIR" ] || mkdir -p "$LOG\_DIR"			-위의 내용 참고**

**date '+%F %T' >> "$LOG\_DIR/sensor.log" 2>\&1		- 날짜**



**# crontab -e :  \* \* \* \* \* /home/user/collect.sh**

**/etc/systemd/system/collect.service — S8**

**sudo vi 파일명.service**

**sudo systemctl daemon-reload \&\& sudo systemctl enable --now collect**





PATH=/user/local/bin:/usr/bin:/bin

LOG\_DIR=/home/ubuntu/logs

\[ -d "$LOG\_DIR" ] || mkdir -p "$LOG\_DIR"

while true; do

{

&#x20;       date '+%F %T' >> "$LOG\_DIR/sensor.log" 2>\&1

}

sleep 5

done



\[Unit]

Description=Day 3 sensor log collector       - 언제?

After=network.target					- 네트워크 먼저

\[Service]

ExecStart=/home/ubuntu/collect\_loop(파일명).sh	이 위치에 파일이 있으면

Restart=on-failure						실패 or 버그 시

RestartSec=5							5초 후 재실행

\[Install]

WantedBy=multi-user.target					이걸 해라
```


\--------------------------------------------------

■ 3. 에러 노트 (Troubleshooting)

\--------------------------------------------------

**./collect.sh`가 만든 변수 LOG\_DIR이 터미널에서 빈 값인 이유는?**

1 자식 프로세스와 함께 소멸해서

2 export 명령을 실행하지 않았기 때문

3 변수 이름이 대문자라서

4 bash 버전 차이





**답:1**

**변수는 프로세스 안에 산다. 자식 bash가 종료되면 그 변수 상자도 사라진다.**





**터미널에서는 되는 명령이 cron에서만 exit 127로 실패할 때 1순위 원인은?**

1 PATH가 최소 환경이라 못 찾음

2 cron이 스크립트를 컴파일해서

3 crond가 1분보다 빨리 돌아서

4 cron 환경에서는 bash를 쓸 수 없기 때문





**답: 1**

**cron 자식의 환경은 PATH=/usr/bin:/bin 수준. 해결: 절대경로 또는 crontab 맨 위 PATH= 지정**



\--------------------------------------------------

■ 4. 질문 및 복습할 내용

\--------------------------------------------------

\- \[질문] (이해가 안 된 코드 줄이나 로직)

\- \[검색] (수업 후 따로 찾아봐야 할 관련 공식 문서나 키워드)



==================================================

