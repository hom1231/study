260828 - 주제 : \[ 운영체제 활용 - 1 ]



==================================================

\[김정인 강사님] 코딩 \& 개념 노트

==================================================

\--------------------------------------------------

■ 1. 오늘 배운 주요 단어 / 문법

\--------------------------------------------------

(형식: \[단어/명령어] : 의미 - 부연설명)



\* \[ **OS(Operating System)** ] : 부품과 프로그램 사이의 관리인

&#x20; > 역할 : **File System 관리, Process 관리, Memory 관리, Device 관리**



\* \[ **GUI(Graphical User Interface)** ] : File Explorer - 마우스로 말 걸기

&#x20; > 활용 예시: **마우스로 프로그램 클릭** / 반복 작업에는 손이 많이 간다.



\* \[ **CLI(Command Line Interface)** ] : 글자로 말 걸기

&#x20; > 활용 예시: **Terminal 키보드로** 명령 / 반복 자동화 원격에 압도적이다.

\* \[ **Drive와 Root** ] : C:\\ —창고의대문. **모든주소가여기서출발**

&#x20; > 활용 예시: Root는 시작(대부분이 드라이브) **leaf는 아래 잔가지**, **Directory는 폴더**

\* \[ **Home Directory** ] : **C:\\Users\\<사용자명> 모든 실습의 기준점**

&#x20;        **폴더 표기규칙**

• 이름에쓸수없는문자:  \\/ : \* ? " < > |

• 공백이든이름은따옴표로감싼다—"My Folder"

• 대소문자구분여부는OS마다다름—Windows 기본은구분하지않음



\* \[ **PATH** ] : 파일의 주소 절대(C:\\Users\\user\\Desktop), 상대(뒤로 가기 등 )

&#x20; > 활용 예시: **상대 주소**에서 C:\\Users\\user\\Desktop **.. 은 한 칸 바깥** 표시 **. 은 현재 폴더** 표시이다.

**..\\..\\면 두칸 바깥의 폴더** **자체**를 의미함

&#x20;

\* \[ BUDDY System ] : 0 1 2 3 페이지 한 페이지 당 4KB If 한 프로세스가 10KB인 경우

그것을 커버할 수 있도록 3페이지를 할당하며 다른 프로세스를 접속할 경우 겹치지 않도록 조정한다.



\* \[ 터미널 명령어 ] : cd(change directory)-현재 위치를 확인한다.  **dir -목록 확인**

&#x20; > 활용 예시: **cd - 위치 확인 cd (path) path로 가자**.



###### \* \[ 터미널 명령어 ] :

|**명령어**|**mkdir <폴더이름>**|**copy <파일이름> <위치>**|**move  <파일이름> <위치>**|**ren <파일이름> <바꿀 이름>**|**del <파일이름>**|
|-|-|-|-|-|-|
|**역할**|**폴더 생성**|**복사 붙여넣기**|**파일 옮기기**|**파일 이름 바꾸기**|**파일 완전 삭제**|



\* \[ Process ] :실행중인 프로그램

&#x20; > 활용 예시:

Ready Queue, Wait Queue, schedule(), irq=0, ls, calc, vi

do\_timer()

&#x09;current -> counter-=10;

TASK\_RUNNING, TASK\_INTERRUPTIABLE

프로세스의 멀티태스킹 구성들

\* \[ 리눅스 ] : 리눅스 기본 명령어
```
**vi a(파일명).c** ( vi 편집기 파일명 **a를 생성**한다)

**gcc a(파일명).c** ( vi편집기 파일 a를 컴파일(업로드)한다.

**./a(파일명).out** ( 파일명 **a를 출력**한다.)

**which** ls (ls **위치 찾는**다,)
```


fork는 분신 ( 기계어 코드는 유지됨 )

execve는 둔갑



\--------------------------------------------------

■ 2. 핵심 코드 \& 로직 흐름

\--------------------------------------------------

\[구현 목표 1 : fork]

↓↓↓ Code ↓↓↓
```
int main()

{


  fork(); < - 프로세스 생성 명령어 - child 프로세스 생성하여 **fork 아래부터 한번 더 실행**함

  printf("hello\\n);

  return 0;

}

```




\--------------------------------------------------

■ 3. 에러 노트 (Troubleshooting)

\--------------------------------------------------



**File System의 Tree 구조를 위에서 아래로 옳게 나열한 것은?**

Drive → Root → Directory → File

File → Directory → Root → Drive

Root → Drive → File → Directory

Directory → Drive → Root → File





**답:1**

**Drive(C:\\) → Root → Directory → File (S3)**





**Path separator를 Windows / macOS·Linux 순서로 옳게 짝지은 것은?**

/ 와 \\

둘 다 \\

둘 다 :

\\ 와 /





**답:4**

**Windows는 \\, macOS·Linux는 / (S3)**





Terminal과 Shell의 구분으로 옳은 것은?

1\.Terminal은 명령을 해석·실행하는 프로그램이고 Shell은 그것을 감싼 창이다

2\.둘은 완전히 같은 말이다

3\.Terminal은 입출력 창이고, Shell은 명령을 해석·실행하는 프로그램이다

4\.Shell은 Windows에만 있는 기능이다





**답:3**

**해설: Terminal = 창, Shell = 해석·실행 프로그램(CMD·PowerShell·bash)**





**move(mv) 명령에 대한 설명으로 옳은 것은?**

이동뿐 아니라 이름 바꾸기에도 쓰인다

파일을 복사하고 원본을 그대로 남긴다

&#x20;directory 목록을 출력한다

파일을 휴지통으로 보내 나중에 복원할 수 있게 한다





**답:1**

**이동과 이름 바꾸기가 같은 명령** 





**따옴표 없이  mkdir My Folder  를 실행하면?**

My Folder 라는 directory 하나가 생긴다

My 와 Folder 두 개의 directory가 생긴다

Shell이 공백을 허용하지 않는다는 에러를 내고 아무것도 생성되지 않는다

My 하나만 생기고 Folder는 무시된다





**답:2**

**해설: Shell은 공백을 인자 구분자로 읽는다 — 따옴표 실험실**



\--------------------------------------------------

■ 4. 질문 및 복습할 내용

\--------------------------------------------------

\- \[질문] (이해가 안 된 코드 줄이나 로직)

\- \[검색] (수업 후 따로 찾아봐야 할 관련 공식 문서나 키워드)



==================================================

