\- 주제 : \[2진수 심화]

==================================================

\[김정인 강사님] 코딩 \& 개념 노트

==================================================

\- 일시 : 2026.08.26



\--------------------------------------------------

■ 1. 오늘 배운 주요 단어 / 문법

\--------------------------------------------------

(형식: \[단어/명령어] : 의미 - 부연설명)



\* \[ OF (Over Flag) ] : **양수 + 양수 = 음수** or 음수 + 음수 = 양수인 경우 On

signed에서 활용, OF!=SF인 경우 앞의 숫자가 작고 OF=SF인 경우 앞의 숫자가 크다.

&#x20; > 활용 예시:

```
char a=100, b=-100;

if (a<b) // a-b **( 100-(-100)=100+100 =200<-127초과로 음수가 됨)**
```




\* \[ SF (Sign Flag) ] : **Signed**가 1(음수) 0(양수) 확인

signed에서 활용, 상단의 OF와 함께 볼 것

&#x20; > 활용 예시:

```
char a=3, b=5;

if (a<b) // a-b (3-5=-2 = \*\*1(S)\*\*111 1110)
```


\* \[ CF (Carry Flag) ] : 최상위 빌림(올림)수

Usigned에서 활용,  CF=0인 경우 앞의 숫자가 크고 0이 아닌경우 앞의 숫자가 작다.

&#x20; > 활용 예시:

unsigned ch a=3, b=5;

if (a<b) // a-b 3-5

cpu가 1 빌려옴(빌림수) - > 빌려왔으므로 CF ON

1111 12

\--------------

0000 0011

\-0000 0101

&#x20;1111 1110



\* \[ ZF (Zero Flag) ] : 계산 결과가 0일 떄 On

&#x20; > 활용 예시:

```
unsigned ch a=3, b=3;

if (a<b) // a-b 3-3 = 0 ZF ON
```


\* \[ OR ] : BIT 세팅(추가)

&#x20; > 활용 예시:



\* \[ AND ] : 마스크 효과 특정 구역 bit **검사** 루틴

&#x20; > 활용 예시:



\* \[ NOT ] : **\~1  중 \~는 비트의 0과 1을 서로 뒤집는 NOT 연산자 즉, 1이었던 맨 끝자리만 0이 되고 나머지 모든 앞자리는 1이된다.**

&#x20; > 활용 예시:

```
item = item \& \~1;
```


\* \[ XOR ] : 토글

&#x20; > 활용 예시:

```
int flag=3, key=flag^5; **- flag는 함수 / ^= 표시는 XOR / key는 3과 5 이진수의 xor값 = 6 / 이로서 flag는 3과 5의 반복이 일어남**=


flag ^= key;



if( flag == 3 )

&#x09;flag=5;

else if (flag == 5 )

&#x09;flag=3:
```






\--------------------------------------------------

■ 2. 복습이 필요한 코드 줄이나 로직

\--------------------------------------------------
```
item = item|1; **( item 이란 인덱스의 정수1 자리에 아이템 세팅하라)**





int item\[4]; **( item 인덱스 32비트가 0,1,2,3, 총 4줄(열)이 존재. )**



int index = 100; **( 아이템 위치 자리 선언 )**



item\[index/32] |= 1<<(index%32));

( 1인덱스=32비트이므로 32로 나눠서 \*\*몫\[인덱스 줄 위치]\*\*과 **나머지\[왼쪽으로 밀어야할 비트**]로 나눔)





for(i=0; i<4; i++) **- ( 0부터 시작해서 4까지 1씩 늘어남 )**

&#x09;if(item\[i] == ( 1<<i )) **- i가 한칸씩 왼쪽으로 밀림**

&#x09;	printf("%d\\n");







int flag=3, key=flag^5; **- flag는 변수 / ^= 표시는 XOR / key는 3과 5 이진수의 xor값 = 6 / 이로서 flag는 3과 5의 반복이 일어남**



flag ^= key; **( flag의 2진수 값을 key와 XOR 시켜서 flag에 저장해라. )**
```




\--------------------------------------------------

■ 2. 핵심 코드 \& 로직 흐름

\--------------------------------------------------

\[구현 목표 1 : (무엇을 만드는 코드인지 작성)]

↓↓↓ Code ↓↓↓

(여기에 코드 복사/붙여넣기)







↑↑↑ Code ↑↑↑

\- 로직 설명 : (이 코드가 어떻게 작동하는지 1\~2줄로 요약)

\- 주의 사항 : (들여쓰기, 오타 주의, 특정 환경 등 강사님 강조 포인트)





\[구현 목표 2 : (무엇을 만드는 코드인지 작성)]

↓↓↓ Code ↓↓↓







↑↑↑ Code ↑↑↑

\- 로직 설명 :

\- 주의 사항 :



\--------------------------------------------------

■ 3. 에러 노트 (Troubleshooting)

\--------------------------------------------------

(코딩 중 발생한 오류와 해결 방법을 적어두면 큰 자산이 됩니다)



\* \[에러 증상 / 메시지] :

&#x20; > 원인 :

&#x20; > 해결 방법 :



\* \[에러 증상 / 메시지] :

&#x20; > 원인 :

&#x20; > 해결 방법 :



\--------------------------------------------------

■ 4. 질문 및 복습할 내용

\--------------------------------------------------

\- \[질문] (이해가 안 된 코드 줄이나 로직)

\- \[검색] (수업 후 따로 찾아봐야 할 관련 공식 문서나 키워드)



==================================================

